{
  "name": "meco",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "test": "cd apps/consumer && poetry run pytest -s",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "turbo": "^2.4.0",
    "typescript": "5.7.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
